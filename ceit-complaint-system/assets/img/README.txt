Put your official Cavite State University logo file here, named exactly:

    logo.png

(Any reasonably square image works best, e.g. 128x128 or 256x256.)

I can't fetch or generate the actual CvSU seal myself (it's the university's
own copyrighted/trademarked mark), so you'll need to save your own copy of
it into this folder. Once the file exists at assets/img/logo.png, the header
will pick it up automatically -- no code changes needed. If the file is
missing, the header falls back to a plain "CvSU" badge so the page still
looks fine either way.
